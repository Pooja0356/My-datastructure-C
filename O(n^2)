#include <stdio.h>
#include <time.h>

int main() {
    int n;
    printf("Enter array size: ");
    scanf("%d", &n);

    int arr[n];
    printf("Enter %d elements:\n", n);
    for(int i = 0; i < n; i++)
        scanf("%d", &arr[i]);

    clock_t start = clock();

    printf("Duplicate values (if any):\n");
    for(int i = 0; i < n; i++) {
        for(int j = i + 1; j < n; j++) {
            if(arr[i] == arr[j])
                printf("%d ", arr[i]);
        }
    }

    clock_t end = clock();
    double time_taken = (double)(end - start) / CLOCKS_PER_SEC;

    printf("\nTime Complexity: O(n^2)\n");
    printf("Execution Time: %f seconds\n", time_taken);

    return 0;
}
